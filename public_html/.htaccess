RewriteEngine On

# Hostinger/Render Optimization
Options -Indexes

# Route Everything to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
